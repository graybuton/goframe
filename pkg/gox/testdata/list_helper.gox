package demo

import gf "github.com/graybuton/goframe/pkg/goframe"

func View(items []string) gf.Node {
	return (
		<ul>
			{gf.For(items, func(item string) gf.Node {
				return gf.El("li", nil, gf.Text(item))
			})}
		</ul>
	)
}
