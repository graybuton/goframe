package demo

import gf "github.com/graybuton/goframe/pkg/goframe"

func View(condition bool, count int) gf.Node {
	return (
		<section>
			{condition ? (
				<p>{count} item(s)</p>
			) : (
				<p>empty</p>
			)}
		</section>
	)
}
