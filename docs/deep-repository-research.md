# GoFrame Deep Repository Research — 2026-08-02

## Stacked Snapshot

This research uses `4955059dfe0b8ea3ed695e6e11bb751b00d44a17`,
the frozen head of `audit/dead-code-and-obsolete-paths`, as its stacked
baseline. The baseline itself is above main
`b71af16cb49a38a5c764b41721b6d719a1c36f0a`. The research branch is
`audit/deep-repository-research`; it was absent from the remote when this work
began. PR #129 was open as a draft at the frozen parent head with its Core,
Browser Smoke, WASM Size, VS Code Extension, and CodeQL checks green and no
unresolved review thread. Issue #117 remained an independent evaluator task.

The initial worktree was clean. Existing ignored VS Code extension build and
package files were preserved. This stage does not alter the parent branch.

## Research Questions

The audit asked where current ownership contracts can be violated even though
the nearest ordinary test succeeds:

- Can a `goxc` command validate less than the production command whose output
  it predicts, mutate before it proves ownership, or publish only part of a
  coordinated package?
- Can accepted GOX syntax generate Go that does not parse, type-check, or build
  once all emitted declarations and literals are considered together?
- Can a failed runtime render mutate state that lifecycle and resource
  rollback otherwise treats as speculative?
- Do host, js/wasm, standard Go, TinyGo, documentation, and CI evidence describe
  the same current contract?

## Architecture And Ownership Map

| Subsystem | Current owner | Invariant traced through production code |
|---|---|---|
| CLI and diagnostics | `cmd/goxc` command handlers | Text/JSON diagnostics, stdout/stderr, and exit status remain command contracts. |
| Source selection | `cmd/goxc` generation and compiler metadata | Build tags and package membership are selected before package generation. |
| GOX package generation | `cmd/goxc` plus `gox.GeneratePackageWithOptions` | All GOX and authored Go files in one package participate in identifier reservation and deterministic rendering. |
| Generated output | `generatePackageTargetsSafely` and the private generated-source publisher | Inputs are generated together, then active writes and managed inactive removals are staged and committed as one recoverable publication plan. |
| Workspace and package publication | `cmd/goxc` workspace/package layers | Physical containment, authored inputs, embeds, compiler execution, and managed package publication have distinct ownership checks. |
| Development generations | `cmd/goxc` dev server | A canonical package is verified, activated as an immutable generation, leased by requests, and reloaded or drained. |
| GOX parser | `pkg/gox` lexer and parser | Markup boundaries, attributes, props, expressions, source positions, and AST shape are accepted or rejected before lowering. |
| GOX code generation | `pkg/gox` generation plans and codegen | Generated source is parser-safe; a successful package result must also survive Go type checking and compilation. |
| Runtime render ownership | `pkg/goframe` component and render transaction | Lifecycle, resource, new state-slot, and reducer-replacement work is speculative until commit. |
| DOM ownership | `pkg/goframe` reconciliation and mount layers | Keyed placement, form state, mounted ranges, and root replacement retain one explicit owner. |

The module floor is Go 1.22. Standard Go 1.22.12, 1.25.12, and 1.26.5,
TinyGo 0.41.1, host tests, js/wasm browser execution, generated bytes, and CLI
schema were treated as separate evidence surfaces.

## Method

The initial call and ownership maps were written outside the repository before
production edits. For each candidate boundary, the nearest tests and history
were read, then an adversarial case and nearby negative control were executed.
Only failures reproduced against the untouched stacked baseline were eligible
for implementation.

Gopls v0.23.0, Staticcheck 2026.1 (`v0.7.0`), deadcode from
`golang.org/x/tools v0.48.0`, and `go vet` supported the call-graph review.
They did not supply the implemented findings. Full Staticcheck runs covered
ordinary, `goframe_debug`, and js/wasm production contexts; their remaining
signals are the already-attributed package-comment, generated-source,
target-stub, exported-surface, and router style findings. No `SA*` correctness
finding appeared. Deadcode used executable and test roots rather than treating
libraries as fictional programs.

Temporary GOX packages, cleanup trees, publication-failure destinations,
relocated binaries, and runtime tests supplied executable evidence. Browser
evidence used real Chrome through the repository aggregate smoke suite. All
scratch sources and generated package trees remain outside version control.

## Independently Discovered Findings

| ID | Provenance | Severity | Subsystem | Contract | Reproduction | Root cause | Disposition |
|---|---|---|---|---|---|---|---|
| DR-01 | `INDEPENDENT_DISCOVERY` | Medium | `pkg/gox`, `cmd/goxc check` | Accepted GOX should not emit duplicate Go fields or ambiguous runtime destinations. | Raw duplicates passed the original baseline; review follow-ups still accepted collisions through synthetic `Children`, DOM aliases, event case, and HTML attribute case. | Raw spelling is not the effective destination: component children add a synthetic field, events normalize separately, and HTML attribute destinations are ASCII case-insensitive before alias mapping. | Fixed in `54a22a65b5e4d2fc1bd0322a2c74cd950d7569bd`, extended in `9777620b0faa4798559f7af71ed256c176c6c302`, and completed in `ed6c96bab1e63c99009a70cf50695a8008440802`. |
| DR-02 | `INDEPENDENT_DISCOVERY` | High | `cmd/goxc clean` | Cleanup may unlink only the adjacent file whose goxc ownership was validated. | The original baseline deleted an unmarked output; after the first correction, replacing or modifying a planned file before deletion still caused the replacement to be removed. | The first plan retained only paths, so deletion could not compare the current file's identity or content with the validated object. | Fixed in `cdeb83c871ea76d36fda887889f2164a40434da0` and completed in `1bbb319ccfd8c3839862bf75ff9790957c7f1c64`. |
| DR-03 | `INDEPENDENT_DISCOVERY` | High | `cmd/goxc` generation publication | A coordinated package generation should not expose a partial new source set after a write failure. | With valid `a.gox` and `z.gox` and a directory at `z.gox.go`, generation returned an error after publishing `a.gox.go`. | Generation computed all bytes first but performed independent atomic file replacements and removals without a recoverable publication boundary. | Fixed in `fe293dcbf64b798fbd61fb12d09793f7e89435bf` with the complete fault matrix in `b60d249a6fa2846e9c9a6951068d61afd4047cd5`. |
| DR-04 | `INDEPENDENT_DISCOVERY` | High | `pkg/goframe` render transaction | A failed render must not commit newly observed state slots or reducer closures. | A failed initial render retained its initial state on retry; a failed reducer rerender changed what an old dispatch closure executed. | `useStateSlot` appended directly to committed `stateSlots`, and `setReducer` replaced the committed reducer during render. | Fixed in `7d0e27b23fd80199cdfe1d260aeb21bede456266`, with browser evidence in `88ff6031360eeb2edd1fdbc2f27855a59342edc6` and the specialized state path in `ea82bf7d1c0e0e46cf9e9cedce813ff57a6597b5`. |

### DR-01: duplicate attributes and props

On the baseline, `GenerateNamed` returned no error for both:

```gox
<div class="first" class="second"></div>
<Button Label="first" Label="second" />
```

`goxc check --format=json` also returned an `OK` report for the DOM case. The
generated package then failed with a duplicate map key or duplicate struct
field. A single attribute or prop generated and built normally, which excludes
a general parser or fixture failure. History showed that duplicate checking was
introduced specifically for `Key`; it never established uniqueness for the
rest of the attribute sequence.

The first correction enforced raw name uniqueness per element while retaining
the existing exact duplicate-`Key` diagnostic. Review follow-up showed that raw
spelling was not the complete destination contract: explicit `Children` plus
renderable nested children emitted two Go fields, while `class`/`className` and
`onClick`/`onclick` reached the same runtime attribute or event.

A second review follow-up found that `htmlFor`/`FOR` still reached the same
browser destination because compiler and runtime normalization covered only a
fixed list of known names. The same mismatch applied to differently cased
custom, `data-*`, and `aria-*` attributes.

Parser and codegen now share one private effective-destination model. Every
non-event HTML attribute destination is ASCII-lowercased before `classname`
maps to `class` and `htmlfor` maps to `for`. Event prefixes remain
case-insensitive with lowercase suffixes, component fields remain
case-sensitive Go names, and non-ASCII bytes are not case-folded. Runtime prop
splitting applies the same attribute rule, including boolean ARIA values.
Direct `Codegen` rejects the same invalid manually constructed ASTs.
Authored-source diagnostics include:

```text
gox: duplicate attribute "class"
gox: duplicate component prop "Label"
gox: explicit Children prop cannot be combined with nested children
gox: attribute "className" conflicts with "class" after normalization
gox: attribute "FOR" conflicts with "htmlFor" after normalization
gox: attribute "data-mode" conflicts with "data-Mode" after normalization
gox: event prop "onclick" conflicts with "onClick" after normalization
```

The JSON check regression verifies schema, source path, authored line and
column, source line, empty stderr, non-success status, and a read-only tree.

### DR-02: unowned adjacent cleanup

On the baseline, an authored `app.gox.go` next to `app.gox` was deleted solely
because its filename matched the legacy output convention. Cleanup had already
removed the requested workspace by the time it reached that adjacent file. A
marked file using the current generated header was the negative control and was
removed as intended.

The first correction planned every requested adjacent removal before mutation.
Review follow-up replaced a planned file before the private deletion phase. The
old loop removed an unowned replacement, an in-place modification, a different
file with identical generated bytes, a symlink, and an empty directory. When a
later plan entry changed, an earlier valid entry was removed first.

Each plan entry now retains its path, `os.FileInfo` identity, and SHA-256 content
fingerprint. The complete planned set is revalidated before adjacent deletion,
then each entry repeats containment, `Lstat` regular-file checks, generated
header validation, `os.SameFile` identity comparison, and digest comparison
immediately before `os.Remove`. A disappeared entry remains a no-op. This is a
portable narrowing of the mutation window, not an atomic filesystem transaction:
a replacement after the final check can still race the unlink.

### DR-03: partial generated-source publication

The reproduction used two valid sources, `a.gox` and `z.gox`, and made the
second destination a directory. The command returned:

```text
goxc: write .../z.gox.go: destination .../z.gox.go is a directory
```

Despite that failure, `a.gox.go` existed as a 179-byte newly generated file
with SHA-256
`b6dfd5c73d5fbd53ef0d2d31a6ef7a6065bf89a432f96797d18ffce9cac39397`.
The negative control with ordinary file destinations published both files.
This differs from the managed package publisher: the defect is adjacent
generated-source publication inside `generatePackageTargetsSafely`.

The generated-source publisher now builds a deterministic plan for active
creations and replacements plus managed inactive removals. It validates every
destination, records existing bytes, permissions, identity, and content
fingerprint, creates required directories, and stages every active output
before visible mutation. Commit revalidates each entry and applies the sorted
plan. A detected failure restores completed mutations in reverse order,
removes newly created outputs, retains managed inactive outputs, and reports
rollback failures together with the primary error. The fault matrix covers
every staging action, every visible mutation position, multiple package
directories, retry, and rollback failure.

This is recoverability for failures detected by the running process, not a
filesystem-wide atomic transaction. Process termination, power loss, hostile
concurrent mutation, and arbitrary rollback failure remain outside the
guarantee. Successful no-active-source cleanup remains committed before the
existing semantic error is returned, and single-file generation still treats
unpublished sibling outputs as read-only verification inputs.

### DR-04: speculative state escapes rollback

The baseline reproduction created state with initial value `1`, then panicked.
A successful retry requesting initial value `99` observed `1`, proving that the
failed attempt left a ghost slot. In a second reproduction, a committed reducer
added `1`; a failed rerender staged a reducer that added `100`. Dispatching
through the old committed closure after recovery produced `101` rather than
`2`.

New state slots and reducer replacements now participate in the shared render
lifecycle boundary. State participation is allocated lazily and finalized
through a specialized state path before generic resource participants and
lifecycle hooks. Failed recover-capable renders discard new slots, make their
new setters and dispatchers inert, and preserve the latest successfully
committed reducer for existing dispatch closures. Protected ErrorBoundary
subtrees retain deferred commit/rollback ownership, including nested boundary
delegation.

Host and standard-Go/WASM failure evidence prove rollback and retry. Standard
Go and TinyGo/WASM both prove successful-path state semantics. TinyGo's current
trap-style panic mode still cannot provide recover-based rollback evidence.
Existing committed state-value updates are not generally rolled back, and this
stage does not add concurrent rendering or scheduler transactionality.

## Historical Hypothesis Disposition

These items came from the 2026-07-25 audit. Reproducing one does not make it an
independent discovery.

| Hypothesis | Provenance | Current-baseline result | Disposition |
|---|---|---|---|
| Typed versus `goframe:`-prefixed invariant panics | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Error-boundary classification still relies on a panic-string prefix, so an application panic can resemble an internal invariant. | Separate runtime error-classification design stage. |
| VirtualList/Table shrink then grow | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Rendering clamps the stored range start but does not commit the clamp; later growth can restore the old distant range. | Separate virtualization-state stage. |
| Generated identifier versus import alias | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Import alias `_goxComponent_view_Button` plus `<Button />` generates a package-scope declaration collision. | Separate package-reservation stage. |
| Unicode GOX identifiers | `HISTORICAL_HYPOTHESIS_CONFIRMED` | A valid Go identifier such as `Привет` reaches byte-oriented tag scanning and fails at a continuation byte. | Language-design stage; no grammar expansion here. |
| Colon in ternary-branch markup | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Text such as `<span>yes: now</span>` inside a ternary is split as syntax and produces an unclosed-tag diagnostic. | Expression-boundary parser stage. |
| Nested GOX callback markup in a prop | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Markup returned inside a callback prop reaches Go expression parsing before nested lowering and fails at `<`. | Nested-expression rewrite stage. |
| Fallback package identity extraction | `HISTORICAL_HYPOTHESIS_CONFIRMED` | `package /*comment*/ probe` falls back to generated identity `main.Button`. | Parser-backed package-identity stage. |
| Comment before GOX markup | `HISTORICAL_HYPOTHESIS_CONFIRMED` | `return /* note */ <main />` is reported as containing no GOX markup. | Markup-discovery stage. |
| Single-quoted attribute escapes | `HISTORICAL_HYPOTHESIS_CONFIRMED` | `title='it\'s'` preserves a backslash form that is not a valid equivalent Go string. | Quoted-literal grammar stage. |
| Leading UTF-8 BOM | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Lowering preserves the leading BOM into a position where generated Go rejects it. | Source-preamble stage. |
| Typed-nil `Codegen` input | `HISTORICAL_HYPOTHESIS_CONFIRMED` | An interface containing `(*gox.Element)(nil)` panics instead of returning an error. | Low-level API contract stage. |
| `BoolValue` versus parser grammar | `HISTORICAL_HYPOTHESIS_SUPERSEDED` | The low-level exported AST can encode `BoolValue`; authored parser syntax continues to require explicit values. No current parser promise says otherwise. | Preserve the two-layer contract. |
| `goxc check` versus package coordination | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Check validates each file's syntax/generation but does not reserve identifiers or type-check the complete package. | Package-aware check stage. |
| Dev reload HTML injection | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Injection remains a case-insensitive last-`</body>` text insertion, as documented. | Structure-aware HTML stage. |
| Size filesystem error propagation | `HISTORICAL_HYPOTHESIS_CONFIRMED` | Size walks can discard traversal/metadata failures and report partial totals. | Filesystem-error stage. |
| `runtime.GOROOT` compiler discovery | `HISTORICAL_HYPOTHESIS_CONFIRMED` | A relocated goxc built with `-trimpath` failed to locate `wasm_exec.js` below an empty runtime root; an auto-downloaded toolchain module also lacked the shim. | Selected-toolchain discovery stage. |
| js/wasm protected-teardown test selection | `EVIDENCE_GAP` | The host-oriented private test bridge does not form a valid js/wasm test program; production browser fixtures remain green. | Correct test-target selection before drawing more conclusions. |
| Documentation and release-contract drift | `NEGATIVE_EVIDENCE` | Documentation checks passed, current release/tag references agree, and no contradictory current contract was found. | No change. |

## Negative Evidence

- Protected-subtree ErrorBoundary transactions, Go 1.26 source-selection
  parity, controlled select reconciliation, context selector topology,
  repeated Mount ownership, document metadata Result D, and PR #129 cleanup
  all retained their focused tests or browser scenarios. No behaviorally new
  reproduction reopened those closed stages.
- A single GOX attribute/prop continues to generate and compile, and duplicate
  `Key` retains its existing diagnostic.
- A marked adjacent output is still removed; absent output remains a no-op;
  unowned `dist` handling is unchanged.
- Gopls found no diagnostic in the changed Go files. Repository-wide loading
  gaps are generated GOX and host/js test-selection gaps already documented by
  the parent audit.
- Staticcheck found no `SA*` issue. Deadcode's remaining test-root output is
  exported `pkg/gox` compatibility surface, not a new private dead path.
- No current dependency, workflow, budget, release, or lockfile contradiction
  was found.

## Implemented Fixes

`54a22a65b5e4d2fc1bd0322a2c74cd950d7569bd` adds per-element duplicate-name
validation, direct generator regressions, a read-only JSON `goxc check`
regression, language-contract wording, and a changelog entry.

`9777620b0faa4798559f7af71ed256c176c6c302` completes DR-01 with effective
component/DOM/event destination validation, defensive direct-codegen checks,
package-generation and JSON diagnostics, and valid parse/type-check/build
controls.

`ed6c96bab1e63c99009a70cf50695a8008440802` completes HTML destination parity
by ASCII-lowercasing every non-event attribute in both GOX validation and
runtime prop splitting, retaining the `className` and `htmlFor` aliases, and
covering boolean ARIA storage under its normalized destination.

`cdeb83c871ea76d36fda887889f2164a40434da0` moves adjacent generated-file
ownership validation ahead of cleanup mutation, tests unmarked/symlinked/
irregular and all-or-nothing preflight cases, and updates the cleanup and
symlink contracts.

`1bbb319ccfd8c3839862bf75ff9790957c7f1c64` completes DR-02 by retaining
filesystem identity and SHA-256 evidence, revalidating the complete plan before
adjacent deletion, and revalidating each entry immediately before unlink.

`fe293dcbf64b798fbd61fb12d09793f7e89435bf` completes DR-03 with a private
generated-source publication plan, complete preflight and staging, deterministic
commit, reverse-order rollback, and an explicitly passed fault-injection hook.
`b60d249a6fa2846e9c9a6951068d61afd4047cd5` supplies the complete staging,
mutation-position, multi-package, retry, no-active-source, single-file, and
rollback-failure matrix.

`7d0e27b23fd80199cdfe1d260aeb21bede456266` makes new state slots and reducer
replacements speculative, `5b9f26cc1dc507689bf0011ccfa69128f2815a8d` supplies the host transaction
matrix, and `c9d3f7b978b6087d46282a0c275533b2d2dec82b` keeps state participation lazy.
`ea82bf7d1c0e0e46cf9e9cedce813ff57a6597b5` finalizes state directly before
generic resource participants rather than converting state to the generic
lifecycle interface. `88ff6031360eeb2edd1fdbc2f27855a59342edc6` adds standard-Go failure/retry
browser evidence, TinyGo successful-path parity, and the measured absolute
size-ceiling alignment.

These fixes do not change a public Go API, GOX output for accepted non-collision
sources, CLI schema, dependencies, workflows, or package format. The completed
DR-04 stage aligns only its measured absolute WASM ceilings; compression ratios
remain unchanged.

## Separate Bounded Stages

### Historical follow-up stages

| Branch | Goal and acceptance boundary | Allowed focus |
|---|---|---|
| `fix/runtime-typed-invariant-classification` | Distinguish internal invariants from application panic strings without changing ordinary error reporting; require focused boundary and browser evidence. | Runtime error types and ErrorBoundary tests. |
| `fix/runtime-virtual-range-restoration` | Commit a valid range after shrink so later growth cannot resurrect stale position; preserve intentional scroll and size behavior. | Virtualization state, tests, focused browser evidence. |
| `fix/gox-import-alias-reservation` | Reserve file import aliases during generated identifier allocation; complete packages with adversarial aliases must build deterministically. | `pkg/gox` allocation and `cmd/goxc` package tests. |
| `research/gox-unicode-identifier-grammar` | Decide whether GOX tag identifiers match Go Unicode identifiers before changing byte-column or grammar contracts. | GOX lexer/parser documentation and tests; no runtime work. |
| `fix/gox-expression-boundaries` | Parse ternary branch text and nested callback markup without colon or nested-`<` ambiguity; require generated package builds and diagnostics. | GOX expression discovery/parser/codegen. |
| `fix/gox-source-preamble-and-package-identity` | Use parser-backed package identity and handle comments/BOM at source boundaries while preserving authored positions. | GOX generation/discovery and diagnostics. |
| `fix/gox-quoted-attribute-escapes` | Define and correctly lower escaped single-quoted values; generated packages must parse and preserve value bytes. | Lexer/parser/codegen and language contract. |
| `fix/gox-codegen-nil-contract` | Return a deterministic low-level error for typed-nil AST nodes without changing valid AST output. | `pkg/gox` Codegen and tests. |
| `fix/goxc-package-aware-check` | Detect package-coordinated allocation/type failures in read-only check while preserving text/JSON schema and no-write behavior. | `cmd/goxc` check plus `pkg/gox` package generation. |
| `fix/goxc-structured-reload-injection` | Replace textual closing-tag discovery with bounded structure-aware insertion while preserving canonical package bytes and reload protocol. | Dev reload injection and tests only. |
| `fix/goxc-size-error-propagation` | Report traversal and metadata errors instead of successful partial size totals; retain exact valid output. | `cmd/goxc/size.go` and CLI tests. |
| `fix/goxc-selected-toolchain-shim-discovery` | Resolve `wasm_exec.js` from the selected Go compiler/toolchain, including relocated goxc and downloaded toolchains. | Compiler environment/discovery, doctor, build tests. |

Each stage stops if it requires an unrelated subsystem, dependency, workflow,
budget change, or an unreviewed public/language contract expansion.

## Output And Size Evidence

For the original deep-research layer through DR-03, unaffected generation modes
were compared with fixed paths. Directory,
single-file, and in-place output remained byte-identical at 554 bytes with
SHA-256
`fec3121e841a23ba10202b3e38ad9b6a78ebf8f79091e8aa55d71c1aa352ec75`.
Existing valid text/JSON diagnostics, stdout, stderr, and exit status were
identical. The package bundle was identical with SHA-256
`8b8b0bff9365ad8144354a6737d9fb9b70b55f782f22865e21f7d57addd44325`;
only the expected `generatedAt` metadata timestamp differed between runs.

For all eleven applications, raw WASM, Brotli, and Zstandard streams were
byte-identical between the stacked baseline and final code head. Gzip payloads
were identical after removing filename/mtime header metadata introduced by
the different temporary build locations. The normalized 44-stream manifest
was identical at
`e60fe91aedaf6db6db7b332c149f783a09689aeab8be9c0f9da4b617adbf0aaa`.

| Application | Raw | gzip | Brotli | Zstandard |
|---|---:|---:|---:|---:|
| counter | 85,716 | 34,391 | 28,747 | 30,901 |
| components | 91,355 | 36,138 | 29,989 | 32,343 |
| todo | 120,621 | 46,716 | 38,617 | 41,587 |
| dashboard | 170,964 | 64,165 | 51,777 | 55,881 |
| context | 117,486 | 44,286 | 36,184 | 39,047 |
| virtualized | 124,418 | 48,044 | 39,226 | 42,489 |
| multipackage | 96,537 | 37,884 | 31,412 | 33,881 |
| cmdapp | 96,555 | 37,891 | 31,428 | 33,877 |
| router | 117,800 | 45,029 | 37,057 | 40,010 |
| router-dashboard | 234,630 | 94,032 | 77,404 | 82,605 |
| resource | 157,459 | 68,679 | 57,813 | 61,514 |

Every absolute and ratio budget at that checkpoint passed without a budget
change. The later DR-04 closeout is recorded in
`docs/wasm-size-headroom-audit.md`: all ratios still pass unchanged, while only
the fifteen measured absolute cells that remained over their ceilings after
state-path specialization move to the next one-KiB boundary.

The DR-03 continuation compared the frozen merge
`f61f0bc97467165a21bb15d069f34762321283bb` with the final code head
`b60d249a6fa2846e9c9a6951068d61afd4047cd5` at fixed absolute paths. Default
multi-package, explicit-output, in-place, active
replacement, single-file, and managed inactive-removal results had identical
filenames, generated bytes, permissions, stdout, stderr, diagnostics, and exit
status. All eleven raw WASM, reproducible gzip, Brotli, and Zstandard streams
were byte-identical. GNU gzip's default header initially differed only by the
source-file mtime; `gzip -n` removed that measurement metadata. The unchanged
size gate passed all absolute and ratio budgets.

## Validation

Tests-first baseline probes observed both implemented failures before
production edits. The new generator/check tests and cleanup tests then failed
for those exact reasons and passed after their respective fixes; no deliberately
red commit was created.

- Raw and effective attribute/prop collision regressions and cleanup ownership
  revalidation passed 100 repetitions and 20 race repetitions. Nearby
  generation/check/clean/path/symlink suites passed under Go 1.22.12, 1.25.12,
  and 1.26.5. Valid effective-destination controls parsed, type-checked, and
  built as a complete generated package.
- Full ordinary and `goframe_debug` tests and vet passed under Go 1.22.12,
  1.25.12, and 1.26.5. Race tests for `./pkg/... ./cmd/...` passed under Go
  1.25.12 and 1.26.5.
- `scripts/check.sh` passed, including generation, package, doctor, benchmark,
  race, vet, and size checks.
- State transaction host tests passed high-count, race, and `goframe_debug`
  runs. Ten fresh standard-Go/WASM runs proved failed-render rollback, retry,
  inert discarded closures, and committed-reducer ownership. Ten fresh
  TinyGo/WASM runs proved the successful state/reducer/resource path without
  intentionally panicking.
- Two aggregate Chrome runs passed. Their dev publication probes observed
  respectively 18/14 batches, 54/42 complete responses, and zero 404 or partial
  responses. Server-backed, history routing, document ownership, runtime error,
  controlled select, context topology, repeated mount, and other aggregate
  scenarios passed in both runs. One earlier attempt was invalidated before
  product behavior because `/usr/bin/go` tried to download Go 1.26.5 while the
  dev probe intentionally set `GOSUMDB=off`; both accepted runs used the
  installed Go 1.26.5 binary with `GOTOOLCHAIN=local`.
- Artifact, module-path, documentation, and actionlint checks passed.
- VS Code extension `npm ci` and tests passed; its lockfile was unchanged.
- `cmd/goxc` cross-compiled for Windows/amd64 with Go 1.26.5. Windows tests were
  not executed locally.
- Final Gopls checks were clean for changed Go files. Full Gopls, Staticcheck,
  and deadcode reruns introduced no new strong finding; known nonzero
  Staticcheck output remains classified above and in the parent audit.

The DR-03 continuation passed its focused generation/publication suites under
Go 1.22.12, 1.25.12, and 1.26.5, 100 high-count repetitions under Go 1.26.5,
and 20 race repetitions under Go 1.25.12 and 1.26.5. Full ordinary,
`goframe_debug`, and vet suites passed under all three Go versions; full race
suites passed under Go 1.25.12 and 1.26.5. `scripts/check.sh`, two complete
Chrome runs, actionlint, the VS Code extension tests, Windows/amd64 compilation,
and all repository gates passed. The Chrome dev publication probe observed no
partial responses during either accepted run.

## Limitations

Research ran on Linux/amd64. Windows received compile evidence, not execution.
TinyGo successful-path state semantics have browser evidence, but its trap-style
panic mode does not supply recover-based failed-render rollback evidence.
Confirmed historical grammar and API questions were reproduced but were not
expanded into public or language decisions here. No remote CI was rerun for the
unpublished top layer.

## Non-Goals

This stage does not repeat PR #129 analyzer cleanup, change public APIs, redesign
GOX grammar, alter runtime identity or scheduling, change dependencies,
workflows, lockfiles, release policy, package format, or WASM budgets. It does
not modify `examples/server-backed`, issue #117, or parent-branch history.
