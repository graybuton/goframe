package main

import gf "github.com/jin-wu/goframe/pkg/goframe"

func App() gf.Node {
	count := gf.UseState(0)

	return (
		<div class="app">
			<h1>Counter: {count.Get()}</h1>
			<button onClick={func() {
				count.Set(count.Get() + 1)
			}}>
				Increment
			</button>
		</div>
	)
}
