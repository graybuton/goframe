package main

import gf "github.com/graybuton/goframe/pkg/goframe"

func App() gf.Node {
	count, setCount := gf.UseState(0)

	return (
		<div class="app">
			<h1>Counter: {count}</h1>
			<button onClick={func() {
				setCount(count + 1)
			}}>
				Increment
			</button>
		</div>
	)
}
